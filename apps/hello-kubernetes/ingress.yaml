apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-kubernetes-ingress
  namespace: default # Deploys to 'default' namespace
spec:
  rules:
    - host: hello-kubernetes.localcluster
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-kubernetes # Service name
                port:
                  number: 80 # Service port
